server.port = 9204

# H2 (Must change enabled=false for production)
spring.h2.console.enabled=false
spring.h2.console.path=/h2
# Datasource
# MR EDITS
# FOR non-persisten use the following
spring.datasource.url=jdbc:h2:./mallikas-db
#;DEFRAG_ALWAYS=TRUE added because .mv.db file grows too big

# FOR persistent, use the following
#spring.datasource.url=jdbc:h2:./mallikas-db;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE
#spring.jpa.hibernate.ddl-auto=update
# END OF PERSISTENCE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
# spring.jpa.hibernate.ddl-auto=none